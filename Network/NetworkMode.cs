namespace Castrimaris.Network {

    /// <summary>
    /// Networking mode in the executing device
    /// </summary>
    public enum NetworkMode { 
        SERVER, 
        CLIENT, 
        HOST, 
        SERVER_LOCAL, 
        CLIENT_LOCAL,
        SERVER_LOCALHOST,
        CLIENT_LOCALHOST
    }

}
